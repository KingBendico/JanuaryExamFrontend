function makeSettings() {
    const URL = "https://benjamincholeva.dk/januar2021eksamen_backend";
    const URL_LOCAL = null;

    return {
        URL,
        URL_LOCAL
    }
}

const Settings = makeSettings();
export default Settings;
